function handler(event) {
    var uri = event.request.uri;
    if (uri === '/pages' || uri.startsWith('/pages/')) {
        return {
            statusCode: 403,
            statusDescription: 'Forbidden'
        };
    }
    return event.request;
}
